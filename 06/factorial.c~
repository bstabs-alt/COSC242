#include <stdio.h>
#include <stdlib.h>

int factorial(int n){
    n = n*factorial(n-1);
}

int main(){
    int n;
    
    while(1 == scanf("%d", &n)){
        printf("%d\n", factorial(n));
    }
    
    return EXIT_SUCCESS;
}
