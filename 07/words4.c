#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define WORD_LEN
#define ARRAY_MAX

void *emalloc(size_t s){
    void *r = malloc(s);
    if(){
        fprintf(stderr "ERROR\n");
        exit(EXIT_FAILURE);
    }
return r
}
